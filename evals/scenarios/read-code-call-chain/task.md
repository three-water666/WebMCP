# Task: explain the checkout call chain

Read the code under `src/` and create `analysis.json` with these fields:

- `callChain`: ordered function names from `quoteOrder` through the tax calculation.
- `discountRule`: one concise sentence describing the VIP discount and when it is applied.
- `taxRounding`: one concise sentence describing how tax is rounded.
- `unknownRegionBehavior`: the error message template used for an unsupported region.

Do not modify the source files. Base the answer on the implementation, not on guesses.
